<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_favorites"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="16dp"
    android:paddingRight="16dp"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="edu.uco.captainplanet.myapplication.FavoritesActivity">

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:id="@+id/txtFavorites"
        android:gravity="center"
        android:textStyle="bold"
        android:text="My Favorites"/>

    <ListView
        android:layout_width="match_parent"
        android:layout_height="220dp"
        android:layout_alignParentStart="true"
        android:id="@+id/lstFavorites"
        android:layout_below="@+id/txtFavorites" />

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:id="@+id/txtPossibleFavorites"
        android:layout_below="@id/lstFavorites"
        android:gravity="center"
        android:textStyle="bold"
        android:text="Possible Favorites"/>

    <ListView
        android:layout_width="match_parent"
        android:layout_height="220dp"
        android:layout_alignParentStart="true"
        android:id="@+id/lstPossibleFavorites"
        android:layout_below="@+id/txtPossibleFavorites" />

</RelativeLayout>
